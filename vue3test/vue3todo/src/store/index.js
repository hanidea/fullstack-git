import { createStore } from 'vuex'

export default createStore({
  state: {
  },
  //第一个参数state 第二个参数是需要修改的值
  mutations: {
  },
  //异步提交mutation
  //第一个参数是store 第二个参数是修改的值
  actions: {
  },
  // 模块化
  modules: {
  }
})
