<view class="content">

    <view class="">
     <image src="{{movie.images.large}}" mode="aspectFit"></image>
    </view>
    <view class="hr"></view>
    <view class="detail">
        <text>{{movie.text}}</text>
    </view>

    <view class="casts">
    <block wx:for="{{movie.casts}}" wx:key="key">
        <image src="{{item.avatars.medium}}" mode="aspectFit"></image>
    </block>

     </view>
     <view class="hr"></view>
    <view class="detail">
        <text>{{movie.summary}}</text>
    </view>


</view>